language: c

install:
  - sh ./.travis.install.deps.sh

script:
  - export RUSTC_FLAGS = -g
  - make
  - make test -j4
  - make install DESTDIR=${PWD}/destdir

env:
  matrix:
    - BITS=32
    - BITS=64

os:
  - linux
  - osx
